# Wymagania dla bot_gemini_test.py — Faza 1 migracji Cascade -> OpenAI Realtime
# (patrz CLAUDE.md), plus doraźny test porównawczy Gemini Live (sekcja
# "-gemini-live" na końcu bot_gemini_test.py, patrz docstring tam).
#
# WAŻNE: pipecat-ai jest tu przypięty na 1.4.0 (nowe API: LLMContext,
# LLMContextAggregatorPair, OpenAIRealtimeLLMService, GeminiLiveLLMService).
# To CELOWO inna wersja niż w requirements.txt (0.0.104), której nadal używa
# produkcyjny bot.py ze starym API (OpenAILLMContext, create_context_aggregator).
#
# NIE instaluj tego pliku w tym samym środowisku/serwisie co bot.py —
# podbicie pipecat-ai wywali produkcję. Ten plik ma sens tylko jako
# requirements dla OSOBNEGO serwisu Railway, który uruchamia
# `uvicorn bot_gemini_test:app`.
#
# OpenAIRealtimeLLMService nie wymaga osobnego extra (paczka `openai` jest
# bazową zależnością pipecat-ai). GeminiLiveLLMService WYMAGA extra [google]
# (paczka google-genai) — bez tego import na górze pliku wywali cały serwis,
# w tym też route'y OpenAI Realtime.

# [google,silero] — silero bo transporty w tym pliku uzywaja SileroVADAnalyzer.
# Wczesniej to dzialalo tylko dzieki recznej komendzie builda na Railway
# ("pip install -r requirements.txt && pip install -U pipecat-ai[google,silero]"),
# ktora w ogole NIE czytala tego pliku — dlatego PyJWT (dopisany nizej dla
# transferu Vonage) nigdy sie nie zainstalowal, mimo ze byl tu od dawna.
# Trzeba zmienic Build Command na Railway na instalacje z TEGO pliku.
pipecat-ai[google,silero]==1.4.0

# Server
fastapi
uvicorn[standard]
python-dotenv
python-multipart

# HTTP (helpers.py gada z Turso po HTTP API)
httpx

# Reużywane przez helpers.py
cryptography

# Vonage REST API (transfer żywej rozmowy) — JWT RS256 podpisywany kluczem
# prywatnym Aplikacji Vonage, patrz realtime_tools.py::build_transfer_tool
PyJWT

# Oficjalny SDK ElevenLabs — WYŁĄCZNIE do wywołania
# conversational_ai.twilio.register_call() w bot_elevenlabs_agent.py
# ("bring your own Twilio" — Twilio zostaje podpięty pod NASZ webhook,
# a to jest jedyne miejsce gdzie MY wołamy ICH API, nie odwrotnie).
# NIE mylić z pipecat-ai[elevenlabs] w requirements.txt (tamto to klient
# websocket do samego streamingu TTS, zupełnie inna zależność/inny cel).
elevenlabs

# Surowy klient WebSocket — most Vonage<->ElevenLabs Conversational AI
# (ElevenLabsRealtimeService w bot_elevenlabs_agent.py, dopisany 2026-09-03).
# Jawnie dopisany mimo że pipecat-ai i tak go ciągnie tranzytywnie (używany
# wewnętrznie przez OpenAIRealtimeLLMService/GeminiLiveLLMService) — bez
# jawnej deklaracji tu, przyszła zmiana pipecat-ai mogłaby po cichu przestać
# go ciągnąć i wywalić WYŁĄCZNIE tę jedną ścieżkę bez oczywistego powodu.
websockets
